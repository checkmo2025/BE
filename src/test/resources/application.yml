spring:
  modulith:
    events:
      completion-mode: DELETE
  profiles:
    active: test
    include:
      - monitoring

server:
  forward-headers-strategy: NATIVE

springdoc:
  use-fqn: true

realtime:
  websocket:
    allowed-origins:
      - http://localhost:3000
